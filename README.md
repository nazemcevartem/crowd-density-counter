# crowd-density-counter
CNN-based crowd counting via density map estimation. TensorFlow implementation of a CSRNet-like architecture (VGG16 encoder + dilated convolutions) with combined pixel-and-count loss. Trained on ShanghaiTech Part A.
